// Achievement persistence is server-authoritative. This module remains only as a
// compatibility export for consumers of the state type.
export { emptyAchievementState, type AchievementState } from "./achievementState";
